9121
.ftco-animate {
  /* opacity: 0; */
  /* visibility: hidden; */
 }
->
 .ftco-animate {
   opacity: 0;
  visibility: hidden;
 }

 import { useNavigate } from 'react-router-dom';
  const navigate = useNavigate();
  navigate('/home');

call only called inside react function

  npm react-route-dom
  npm install antd --save
  npm install react-bootstrap-validation --save
  npm install axios
  npm i --save react-bootstrap
  npm install react-redux
//  npm install devextreme-react/scheduler
npm install formik yup
npm install formik yup
npm install @mui/icons-material @mui/material @emotion/styled @emotion/react

npm install devextreme@23.1 devextreme-react@23.1 --save --save-exact
only per file one defualt export possible

ui -formik  yup


// cart loading scrollbar
// register navigate after show you register success
// able to edit that today calories
//


